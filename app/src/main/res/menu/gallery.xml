<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/addImage"
        android:icon="@drawable/ic_add_image"
        app:showAsAction="always"
        android:title="Add Image"/>

    <item
        android:id="@+id/addImageFromGallery"
        android:icon="@drawable/ic_baseline_add_image_from_gallery"
        app:showAsAction="always"
        android:title="Add Image From Gallery"/>

    <item
        android:id="@+id/sortAlpha"
        android:icon="@drawable/ic_sort_by_alpha"
        android:title="SortAlpha"
        app:showAsAction="always"/>

    <item
        android:id="@+id/search"
        android:title="@string/search"
        android:icon="@drawable/ic_baseline_search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />


</menu>